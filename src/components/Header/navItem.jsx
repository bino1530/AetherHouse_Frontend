// import { useEffect, useRef, useState } from "react";
// import { Link } from "react-router-dom";
// import { CATEGORIES } from "../../data/categories.jsx";
// const NavItem = ({root}) => {
//     const [open , setOpen] = useState(false);
//     const wrapRef = useRef(null)

//     const byCategory = CATEGORIES.filter(
//         (c) => c.parent === item.slug && c.kind === "category"
//     );
//     const byRoom = CATEGORIES.filter(
//         (c) => c.parent === item.slug && c.kind === "room"
//     );
//     const hasChildren = byCategory.length > 0 || byRoom.length > 0 ;

//     useEffect(() => {
        
//     })
// }